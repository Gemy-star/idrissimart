# دليل التحكم في شاشة التحميل - Loader Customization Guide

## 📍 مكان التحكم في شاشة التحميل

يمكنك التحكم الكامل في شكل وحركة شاشة التحميل من خلال ملف CSS الرئيسي:
**الملف:** `static/css/style.css`

ابحث عن قسم `/* PRELOADER STYLES */`

---

## 🎯 الفكرة والمعنى

تم تصميم شاشة التحميل بشكل دائري مع **6 نقاط تدور** حول اللوجو لتوصيل معنى:
**"تجميع السوق في منصة واحدة"** 🌍➡️🎯

كل نقطة تمثل جزء من السوق، وهي تدور حول اللوجو (المنصة المركزية) لتوحيدها.

---

## 🎨 المتغيرات المتاحة للتخصيص

في بداية قسم Preloader، ستجد قسم `:root` الذي يحتوي على جميع المتغيرات:

```css
:root {
    /* أحجام شاشة التحميل */
    --loader-container-size: 140px;       /* حجم الحاوية الكلية */
    --loader-circle-size: 120px;          /* حجم الدائرة المستديرة */
    --loader-image-size: 70px;            /* حجم صورة اللوجو */
    --loader-orbit-dot-size: 10px;        /* حجم النقاط الدوارة */
    --loader-orbit-radius: 70px;          /* نصف قطر مدار النقاط */

    /* سرعة الحركة */
    --loader-orbit-rotation-speed: 15s;   /* سرعة دوران النقاط (كلما قل الرقم، زادت السرعة) */
    --loader-pulse-speed: 2s;             /* سرعة نبضات النقاط */

    /* الألوان */
    --loader-gradient-start: var(--primary-color);
    --loader-gradient-end: var(--secondary-color);
    --loader-shadow-color: rgba(107, 76, 122, 0.3);
    --loader-dot-shadow: rgba(107, 76, 122, 0.6);
}
```

---

## ⚙️ أمثلة على التخصيص

### 1️⃣ تكبير شاشة التحميل
```css
--loader-circle-size: 150px;     /* بدلاً من 120px */
--loader-image-size: 90px;       /* بدلاً من 70px */
--loader-orbit-radius: 85px;     /* بدلاً من 70px */
```

### 2️⃣ تصغير شاشة التحميل
```css
--loader-circle-size: 100px;     /* بدلاً من 120px */
--loader-image-size: 60px;       /* بدلاً من 70px */
--loader-orbit-radius: 60px;     /* بدلاً من 70px */
```

### 3️⃣ زيادة سرعة الدوران (حركة أسرع)
```css
--loader-orbit-rotation-speed: 8s;   /* بدلاً من 15s */
--loader-pulse-speed: 1s;            /* بدلاً من 2s */
```

### 4️⃣ تقليل سرعة الدوران (حركة أبطأ)
```css
--loader-orbit-rotation-speed: 25s;  /* بدلاً من 15s */
--loader-pulse-speed: 3s;            /* بدلاً من 2s */
```

### 5️⃣ تكبير النقاط الدوارة
```css
--loader-orbit-dot-size: 14px;       /* بدلاً من 10px */
```

### 6️⃣ تصغير النقاط الدوارة
```css
--loader-orbit-dot-size: 7px;        /* بدلاً من 10px */
```

### 7️⃣ تقريب النقاط من اللوجو
```css
--loader-orbit-radius: 55px;         /* بدلاً من 70px */
```

### 8️⃣ إبعاد النقاط عن اللوجو
```css
--loader-orbit-radius: 90px;         /* بدلاً من 70px */
```

---

## 🎨 تغيير الألوان

### الألوان للثيم الفاتح (Light Theme)
```css
:root {
    --loader-gradient-start: #6B4C7A;   /* لون البداية */
    --loader-gradient-end: #9b59b6;     /* لون النهاية */
    --loader-shadow-color: rgba(107, 76, 122, 0.3);
    --loader-dot-shadow: rgba(107, 76, 122, 0.6);
}
```

### الألوان للثيم الداكن (Dark Theme)
```css
[data-theme="dark"] {
    --loader-gradient-start: #7c3aed;   /* بنفسجي مشرق */
    --loader-gradient-end: #a78bfa;     /* بنفسجي فاتح */
    --loader-shadow-color: rgba(167, 139, 250, 0.3);
    --loader-dot-shadow: rgba(167, 139, 250, 0.6);
}
```

---

## 📝 ملاحظات مهمة

1. **عدد النقاط:** حالياً 6 نقاط. لإضافة أو تقليل عدد النقاط، عليك تعديل HTML و CSS معاً.

2. **تزامن الأحجام:**
   - احرص على أن يكون `--loader-container-size` أكبر من `--loader-circle-size`
   - احرص على أن يكون `--loader-circle-size` أكبر من `--loader-image-size`

3. **نصف قطر الدوران:**
   - يجب أن يكون `--loader-orbit-radius` أصغر من نصف `--loader-container-size`
   - مثال: إذا كانت الحاوية 140px، فأقصى radius آمن هو 70px

4. **الأداء:**
   - سرعات منخفضة جداً (أقل من 5s) قد تسبب تشويش بصري
   - السرعة المثالية بين 10s - 20s

---

## 🔧 استكشاف المشاكل

### المشكلة: النقاط تخرج عن الشاشة
**الحل:** قلل قيمة `--loader-orbit-radius`

### المشكلة: الحركة سريعة جداً
**الحل:** زد قيمة `--loader-orbit-rotation-speed`

### المشكلة: اللوجو صغير جداً
**الحل:** زد قيمة `--loader-image-size`

### المشكلة: النقاط غير واضحة
**الحل:** زد قيمة `--loader-orbit-dot-size` أو غير لون `--loader-dot-shadow`

---

## 🌐 التحكم في المظهر عبر الثيمات

شاشة التحميل تستجيب تلقائياً للثيم المختار (فاتح/داكن):

```css
/* الثيم الفاتح */
[data-theme='light'] .loader-logo-circle {
    background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
}

/* الثيم الداكن */
[data-theme='dark'] .loader-logo-circle {
    background: linear-gradient(135deg, #7c3aed, #a78bfa);
}
```

---

## 📂 الملفات ذات الصلة

- **HTML:** `templates/partials/_pre_loader.html`
- **CSS:** `static/css/style.css` (قسم PRELOADER STYLES)
- **JavaScript:** تلقائي (يتم إخفاء الـ loader عند تحميل الصفحة)

---

## ✨ نصائح للتصميم المثالي

1. **التناسق:** استخدم نفس ألوان الموقع في المتغيرات
2. **البساطة:** لا تبالغ في عدد النقاط أو السرعة
3. **الوضوح:** تأكد من وضوح اللوجو في المنتصف
4. **الاختبار:** جرب التصميم على شاشات مختلفة

---

تم التحديث: مايو 2024
