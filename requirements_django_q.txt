# Django Q and Redis Requirements
# Add these to your requirements.txt or install directly

# Django Q2 - Task Queue
django-q2>=1.6.0

# Redis - Message Broker for Django Q
redis>=5.0.0

# Optional but recommended
hiredis>=2.3.0  # Faster Redis client

# For monitoring (optional)
# django-q-sentry>=0.1.0  # Sentry integration
# django-redis>=5.4.0     # Redis caching backend

# =======================
# Installation Commands
# =======================

# Using pip:
# pip install django-q2 redis hiredis

# Using poetry:
# poetry add django-q2 redis hiredis

# In Docker:
# docker-compose exec web pip install django-q2 redis hiredis
# docker-compose restart

# =======================
# Verification
# =======================

# Test Redis connection:
# python manage.py shell
# >>> import redis
# >>> r = redis.Redis(host='localhost', port=6379, db=0)
# >>> r.ping()
# True

# Test Django Q:
# python manage.py qcluster
# Should start without errors

# =======================
